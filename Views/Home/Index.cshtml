@model IEnumerable<Product>

@{
    ViewData["Title"] = "خانه";
}

<div class="text-center">
    <h1 class="display-4">به یو مارکت خوش آمدید</h1>

    <div class="row">
        @foreach (var item in Model)
        {
           <partial name="_BoxProduct" model="@item" />
        }
    </div>
 </div>